<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.reservation.dao.ReservationMapper">  
  	<select id="selectList" parameterType="map"
  	                         resultType="reservationVO">
  		SELECT
  		   *
  		FROM (SELECT
  		        a.*,
  		        rownum rnum
  		      FROM (SELECT
  		              b.reserve_num,
  		              <![CDATA[
  		              REPLACE(REPLACE(b.reserve_title,'<','&lt;'),'>','&gt;') reserve_title,
  		              ]]>
  		              b.reserve_date,
  		              b.reserve_room,
  		              b.reserve_room_seat,
  		              b.mem_num,
  		              d.mem_name
  		            FROM reservation b JOIN member_detail d
  		            ON b.mem_num = d.mem_num
  		            <where>
						<if test="keyword != null and keyword != ''">
							<if test="keyfield == 1">
								b.reserve_title LIKE '%' || #{keyword} || '%'
							</if>
							<if test="keyfield == 2">
								d.mem_name LIKE '%' || #{keyword} || '%'
							</if>
							<!-- <if test="keyfield == 3">
								b.content LIKE '%' || #{keyword} || '%'
							</if>
							<if test="keyfield == 4">
								b.title LIKE '%' || #{keyword} || '%' OR
								b.content LIKE '%' || #{keyword} || '%'
							</if> -->
						</if>
					</where> 
  		            ORDER BY b.reserve_num DESC)a)
  		<![CDATA[
  		WHERE rnum >= #{start} AND rnum <= #{end}
  		]]>
  	</select>

  	<!-- 글수정
  	<update id="updateBoard" parameterType="boardVO">
  		UPDATE spboard SET
  		    <if test="filename != ''">
  		    uploadfile=#{uploadfile},
  		    filename=#{filename},
  		    </if>
  			title=#{title},
  			content=#{content},
  		    ip=#{ip},
  		    modify_date=SYSDATE
  		WHERE board_num=#{board_num}
  	</update> -->

</mapper>







